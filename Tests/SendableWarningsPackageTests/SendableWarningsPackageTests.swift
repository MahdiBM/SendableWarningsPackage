import XCTest
@testable import SendableWarningsPackage

final class SendableWarningsPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SendableWarningsPackage().text, "Hello, World!")
    }
}
